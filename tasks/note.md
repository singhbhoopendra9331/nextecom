Tags & categories — DB and server actions support them (create-post.ts accepts tags/categories), but the post form does not expose them. There is no admin UI to manage tags/categories.
SEO / Meta fields — Meta model exists and frontend reads description / meta_description, but page/post forms do not let you edit meta in the admin.
Custom slugs — slugs are auto-generated from titles via slugify; no manual slug field in forms.
Page title — marked in-progress in tasks/todos.md.
Redirects — still todo.

add search on posts on admin page
add searcon on pages on admin page

related posts
add connection test